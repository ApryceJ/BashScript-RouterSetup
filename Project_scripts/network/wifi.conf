#declare array
declare -A wifi
#making devcname an array
devwf=$(ls /sys/class/net | grep w)
IFS=' ' read -r -a wifidev <<< $devwf

#if [ -z $wifidev[@] ] then

  #echo "No Wifi adapter present, will not continue with Wifi Setup"
  #candowifi=1 # will be used by main to skip the rest of wifi setup

#else

# wireless
wifi[${wifidev[0]},0]=10.16.9.254
wifi[${wifidev[0]},1]=25
wifi[${wifidev[0]},2]=NASP21_9
#fi
